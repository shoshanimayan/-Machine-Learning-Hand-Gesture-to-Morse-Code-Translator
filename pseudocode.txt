train_num = 10000
training_data = []
keys = list(d.keys())
for i in range(train_num):
	rand = random.randint(0, 49999)
	k = keys[rand]
	training_data.append(d[k])
	d.pop(k)
	keys.remove(k)

training_labels = []
for data in training_data:
	if [10, 10] in data:
		training_labels.append(0)
	else:
		training_labels.append(1)

clf = MLPClassifier(hyperparameters = "foobar")
clf.fit(training_data, training_labels)

test_num = 2000
test_data = []
for i in range(test_num):
	rand = random.randint(0, 49999 - train_num)
	k = keys[rand]
	test_data.append(d[k])

test_labels = []
for data in test_data:
	if [10, 10] in data:
		test_labels.append(0)
	else:
		test_labels.append(1)

predictions = clf.predict(test_data)

s = 0
for i in range(test_num):
	if predictions[i] == test_labels[i]:
		s += 1

accuracy = s / N